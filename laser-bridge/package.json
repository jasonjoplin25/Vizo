{
  "name": "laser-bridge",
  "version": "1.0.0",
  "description": "WebSocket → UDP bridge for LaserCube hardware",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "build": "tsc --outDir dist",
    "start:compiled": "node dist/server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.14",
    "tsx": "^4.19.0",
    "typescript": "~5.9.0"
  }
}
